providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "libGL", "glib"]

[phases.install]
cmds = [
  "cd backend && pip install -r requirements.txt --no-deps && pip install opencv-python-headless"
]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
